# data/cache

Generated output of `scraper/pokeapi/fetch_all.py`. Not hand-edited — regenerate with:

```
cd scraper
.venv\Scripts\python.exe pokeapi\fetch_all.py
```

Use `--limit N` for a quick smoke test against a small slice of the dex instead of the full pull.
